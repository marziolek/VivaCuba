{% extends "layouts/base.twig" %}

{% block content %}
<div class="row u-margin-top--2">
  <section class="col-xs-12 col-sm-6 posts">
    <a href="{{ function('get_post_type_archive_link', 'post') }}" class="h1">Aktualności</a>
    {% for post in posts %}
      {% include ['components/tease.twig'] %}
    {% endfor %}
  </section>
  <section class="col-xs-12 col-sm-6 posts">
    <a href="{{ function('get_post_type_archive_link', 'event') }}" class="h1">Wydarzenia</a>
    {% for post in events %}
      {% include ['components/tease-event.twig'] %}
    {% endfor %}
  </section>
</div>
{% endblock %}
